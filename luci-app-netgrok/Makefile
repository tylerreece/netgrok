include $(TOPDIR)/rules.mk
include $(TOPDIR)/feeds/luci/luci.mk

LUCI_TITLE := LuCI NetGrok Application -- for use with SSLsplit-NetGrok

define Package/luci-app-netgrok
	SECTION := luci
	CATEGORY := LuCI
	TITLE := LuCI NetGrok Application -- for use with SSLsplit-NetGrok
endef

# call BuildPackage - OpenWrt buildroot signature
